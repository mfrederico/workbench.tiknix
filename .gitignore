/vendor/
conf/config.ini
conf/*.ini
!conf/*.example.ini
/data/
*.db
.DS_Store

# AI Projects (workspace) sidecar: per-instance task data, never tracked
data/workbench.db

# sidecar error logs (per Kernel installLogger)
logs/

# Member-uploaded images attached to tasks and comments — content, not code.
# 9.5MB of it was sitting untracked; one `git add -A` would have committed the lot.
public/uploads/

# Playwright output.
test-results/
playwright-report/
